name: ci

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      test_filter:
        description: Optional Gradle --tests filter (e.g. com.dpis.module.*Hook*Test)
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        shell: bash
        run: |
          (yes || true) | sdkmanager --licenses > /dev/null
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.1.0"

      - name: Ensure gradlew is executable
        run: chmod +x gradlew

      - name: Build and verify
        shell: bash
        env:
          TEST_FILTER: ${{ github.event.inputs.test_filter }}
        run: |
          if [[ -n "${TEST_FILTER}" ]]; then
            ./gradlew :app:assembleModern101Debug :app:assembleCompat100Debug :app:testDebugUnitTest --tests "${TEST_FILTER}" :app:lintModern101Debug --stacktrace
          else
            ./gradlew :app:assembleModern101Debug :app:assembleCompat100Debug :app:testAllDebugUnitTests :app:lintModern101Debug --stacktrace
          fi

      - name: Upload test and lint reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-reports-${{ github.run_id }}
          if-no-files-found: warn
          path: |
            app/build/reports/tests/testDebugUnitTest
            app/build/reports/tests/testModern101DebugUnitTest
            app/build/reports/tests/testCompat100DebugUnitTest
            app/build/test-results/testDebugUnitTest
            app/build/test-results/testModern101DebugUnitTest
            app/build/test-results/testCompat100DebugUnitTest
            app/build/reports/lint-results-modern101Debug.html
            app/build/reports/lint-results-modern101Debug.xml

      - name: Upload PR debug APKs
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: pr-debug-apks-${{ github.event.pull_request.head.sha }}
          if-no-files-found: error
          retention-days: 7
          path: |
            app/build/outputs/apk/modern101/debug/*.apk
            app/build/outputs/apk/compat100/debug/*.apk
