name: CI Check

on:
  pull_request:
    paths-ignore:
      - '**.md'
      - 'docs/**/*.txt'
      - '.editorconfig'
      - '.gitignore'
      - 'docs/**'
      - '.github/**'
      - '!.github/workflows/**'
  workflow_dispatch:
    inputs:
      test_filter:
        description: Optional Gradle --tests filter (e.g. com.dpis.module.*Hook*Test)
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Each job receives an isolated runner and no `needs` dependency, so build, tests,
  # and lint overlap without concurrent Gradle processes contending for one workspace.
  build:
    if: github.event_name != 'pull_request' || !startsWith(github.head_ref, 'release-please--')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      CCACHE_BASEDIR: ${{ github.workspace }}
      CCACHE_COMPILERCHECK: '%compiler% -dumpmachine; %compiler% -dumpversion'
      CCACHE_HARDLINK: 'true'
      CCACHE_NOHASHDIR: 'true'
      CMAKE_C_COMPILER_LAUNCHER: ccache
      CMAKE_CXX_COMPILER_LAUNCHER: ccache
      DPIS_CMAKE_VERSION: 3.22.1
      DPIS_CMAKE_COMPILER_LAUNCHER: ccache
      DPIS_NDK_VERSION: 28.2.13676358
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Install Android SDK packages
        shell: bash
        run: |
          (yes || true) | sdkmanager --licenses > /dev/null
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.1.0" "cmake;${DPIS_CMAKE_VERSION}" "ndk;${DPIS_NDK_VERSION}"

      - name: Install native build cache
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y ccache
          ccache -o max_size=1G
          ccache -o hash_dir=false
          ccache -o hard_link=true
          ccache -o compiler_check="$CCACHE_COMPILERCHECK"
          ccache -z

      - name: Cache native compilation
        uses: actions/cache@v6
        with:
          path: |
            ~/.cache/ccache
            ~/.ccache
          key: native-ccache-${{ runner.os }}-${{ hashFiles('app/src/main/cpp/**', 'app/build.gradle.kts', 'gradle/libs.versions.toml') }}-${{ github.sha }}
          restore-keys: |
            native-ccache-${{ runner.os }}-${{ hashFiles('app/src/main/cpp/**', 'app/build.gradle.kts', 'gradle/libs.versions.toml') }}-
            native-ccache-${{ runner.os }}-

      - name: Ensure gradlew is executable
        run: chmod +x gradlew

      - name: Build debug APKs
        shell: bash
        run: |
          ./gradlew :app:assembleModernDebug :app:assembleLegacyDebug \
            --no-daemon --no-configuration-cache --stacktrace

      - name: Show native cache statistics
        if: always()
        shell: bash
        run: |
          ccache -s

      - name: Summarize PR debug APK checksums
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          {
            echo "### PR debug APK checksums"
            echo "| Package | SHA256 |"
            echo "|:--|:--|"
            for apk in app/build/outputs/apk/modern/debug/*.apk app/build/outputs/apk/legacy/debug/*.apk; do
              [[ -f "$apk" ]] || continue
              package="$(basename "$apk")"
              echo "| ${package} | \`$(sha256sum "$apk" | awk '{print $1}')\` |"
            done
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload PR debug APKs
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: pr-debug-apks-${{ github.event.pull_request.head.sha }}
          if-no-files-found: error
          retention-days: 7
          path: |
            app/build/outputs/apk/modern/debug/*.apk
            app/build/outputs/apk/legacy/debug/*.apk

  unit-tests:
    if: github.event_name != 'pull_request' || !startsWith(github.head_ref, 'release-please--')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Install Android SDK packages
        shell: bash
        run: |
          (yes || true) | sdkmanager --licenses > /dev/null
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.1.0"

      - name: Ensure gradlew is executable
        run: chmod +x gradlew

      - name: Run unit tests
        shell: bash
        env:
          TEST_FILTER: ${{ github.event.inputs.test_filter }}
        run: |
          if [[ -n "${TEST_FILTER}" ]]; then
            ./gradlew :app:testModernDebugUnitTest --tests "${TEST_FILTER}" --no-daemon --no-configuration-cache --stacktrace
            ./gradlew :app:testLegacyDebugUnitTest --tests "${TEST_FILTER}" --no-daemon --no-configuration-cache --stacktrace
          else
            ./gradlew :app:testModernDebugUnitTest --no-daemon --no-configuration-cache --stacktrace
            ./gradlew :app:testLegacyDebugUnitTest --no-daemon --no-configuration-cache --stacktrace
          fi

      - name: Upload unit-test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-reports-${{ github.run_id }}
          if-no-files-found: warn
          path: |
            app/build/reports/tests/testModernDebugUnitTest
            app/build/reports/tests/testLegacyDebugUnitTest
            app/build/test-results/testModernDebugUnitTest
            app/build/test-results/testLegacyDebugUnitTest

  lint:
    if: github.event_name != 'pull_request' || !startsWith(github.head_ref, 'release-please--')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Install Android SDK packages
        shell: bash
        run: |
          (yes || true) | sdkmanager --licenses > /dev/null
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.1.0"

      - name: Ensure gradlew is executable
        run: chmod +x gradlew

      - name: Run Modern lint
        shell: bash
        run: ./gradlew :app:lintModernDebug --no-daemon --no-configuration-cache --stacktrace

      - name: Upload lint reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lint-reports-${{ github.run_id }}
          if-no-files-found: warn
          path: |
            app/build/reports/lint-results-modernDebug.html
            app/build/reports/lint-results-modernDebug.xml
