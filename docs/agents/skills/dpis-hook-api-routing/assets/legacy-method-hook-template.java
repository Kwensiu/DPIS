// Template only. Replace names, targets, and comments with task-specific code.
public final class LegacyMethodHookTemplate {
    // Keep Legacy entry and callback logic in the legacy flavor.
    // Use shared helpers only for framework-independent semantics.
}
