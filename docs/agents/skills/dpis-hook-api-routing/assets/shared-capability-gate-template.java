// Template only. Replace names, targets, and comments with task-specific code.
public final class SharedCapabilityGateTemplate {
    // Centralize capability detection only when the concept is truly shared.
    // Return a decision that flavor-owned code can consume without hiding ownership.
}
